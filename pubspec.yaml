name: mysterylane
description: "MysteryLane"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.3
  http: ^1.6.0
  supabase_flutter: ^2.17.2
  image_picker: ^1.1.2

  google_maps_flutter: ^2.5.0
  google_maps_apis: ^5.1.0
  flutter_polyline_points: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - env/dev.json
